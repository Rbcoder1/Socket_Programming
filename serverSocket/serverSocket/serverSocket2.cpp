#include "stdafx.h"
#include<iostream>
#include<WinSock.h>
using namespace std;

int main2()
{
	//Initilized WSA variables 
	//Initilized Socket 
	int sock = socket(AF_INET,SOCK_STREAM,);
	//Bind socket with local port
	//Listen incomming request and store in waiting queue

		return 0;
}